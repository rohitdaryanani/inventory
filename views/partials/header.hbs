<div class="header">
	<div class="left-nav">
		<h1>Inventory</h1>
		<span>version 1.0</span>
	</div>
	<div class="right-nav">
		{{#if user}}
			<nav>Welcome {{user}}</nav>
		{{else}}
			<nav>Welcome ( Guest ) <a href="/">Sign Up</a> | <a href="/login">Log In</a></nav>
		{{/if}}
	</div>
</div>
